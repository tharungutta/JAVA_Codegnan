package Control_Statements;

public class IfDemo {
  public static void main(String[] args) {
//	  if(1) {
//		                                       // The condition must be of boolean type or else we get error, we can't use bitwise operators in condition .	
//	  }
	if(true) {
		System.out.println("Statement is printed");
	}
}
}
